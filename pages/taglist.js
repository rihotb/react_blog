import styled from "styled-components";

export const TaglistStyles = styled.div`
  text-align: center;
`;

const Taglist = () => (
  <TaglistStyles>
    <p>タグ一覧</p>
  </TaglistStyles>
);

export default Taglist;
